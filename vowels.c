#include<stdio.h>
#include<conio.h>
void main()
{
char a;
printf("enter the alphabets":\a);
scanf("%d",&a);
if(the alphabet a,e,i,o,u)
printf("the alphabets are vowels");
else
if(except the alphabet a,e,i,o,u)
printf("the alphabets are consonants)
getch();
       }  
  

